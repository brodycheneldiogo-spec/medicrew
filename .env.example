EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_EAS_PROJECT_ID=

# Supabase Edge Function secrets (set with `supabase secrets set`, never ship in the app)
# Server-only Supabase Edge Function secrets (never EXPO_PUBLIC_*).
# STRIPE_RESTRICTED_KEY=
# STRIPE_WEBHOOK_SECRET=
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=MediCrew <notifications@medicrew.app>
# PUSH_DISPATCH_SECRET=

# Google OAuth is configured in Supabase Auth > Providers and uses the MediCrew redirect scheme.
# SMS verification is configured in Supabase Auth > Phone providers.
