import { useEffect, useState } from 'react';
import { Alert, KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { SafeAreaView } from 'react-native-safe-area-context';
import { router, useLocalSearchParams } from 'expo-router';
import * as WebBrowser from 'expo-web-browser';
import * as Linking from 'expo-linking';
import { colors, radii } from '../lib/theme';
import { MediCrewLogo } from '../lib/brand';
import { supabase } from '../lib/supabase';
WebBrowser.maybeCompleteAuthSession();
function normalizePhone(value:string){return value.replace(/[\s()-]/g,'')}
function validEmail(value:string){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value.trim())}
function strongPassword(value:string){return value.length>=8&&/[A-Z]/.test(value)&&/[a-z]/.test(value)&&/\d/.test(value)}
export default function Auth(){
 const{role:requestedRole}=useLocalSearchParams<{role?:'professional'|'company'}>();const[mode,setMode]=useState<'signup'|'signin'>('signup');const[step,setStep]=useState<1|2>(1);const[phone,setPhone]=useState('');const[code,setCode]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[showPassword,setShowPassword]=useState(false);const[acceptedLegal,setAcceptedLegal]=useState(false);const[otpSent,setOtpSent]=useState(false);const[loading,setLoading]=useState(false);const[oauthBusy,setOauthBusy]=useState(false);
 useEffect(()=>{if(!supabase)return;const sub=supabase.auth.onAuthStateChange(async(event,session)=>{if(event!=='SIGNED_IN'||!session?.user)return;const role=(session.user.user_metadata?.role||requestedRole) as 'professional'|'company'|undefined;if(!role)return;await supabase.from('profiles').update({email:session.user.email||null}).eq('id',session.user.id);});return()=>sub.data.subscription.unsubscribe()},[requestedRole]);
 async function sendCode(){if(!supabase)return Alert.alert('MediCrew is not configured','Connect Supabase before using authentication.');if(!requestedRole)return Alert.alert('Choose an account type','Go back and choose Professional or Company.');const clean=normalizePhone(phone.trim());if(!/^\+[1-9]\d{7,14}$/.test(clean))return Alert.alert('Invalid phone number','Use international format, for example +33 6 12 34 56 78.');setLoading(true);const{error}=await supabase.auth.signInWithOtp({phone:clean,options:{data:{role:requestedRole,signup_stage:'phone_verified'}}});setLoading(false);if(error)return Alert.alert('Code could not be sent',error.message);setOtpSent(true)}
 async function verifyPhone(){if(!supabase)return;const clean=normalizePhone(phone.trim());if(!/^\d{4,8}$/.test(code.trim()))return Alert.alert('Invalid code','Enter the verification code you received.');setLoading(true);const{data,error}=await supabase.auth.verifyOtp({phone:clean,token:code.trim(),type:'sms'});if(error){setLoading(false);return Alert.alert('Verification failed',error.message)}const role=(data.user?.user_metadata?.role||requestedRole) as 'professional'|'company'|undefined;if(!role){await supabase.auth.signOut();setLoading(false);return Alert.alert('Account setup incomplete','Your account does not have a valid MediCrew role.')}setLoading(false);setStep(2)}
 async function finishSignup(){if(!supabase||!requestedRole)return;const cleanEmail=email.trim().toLowerCase();if(!validEmail(cleanEmail))return Alert.alert('Email required','Enter a valid email address.');if(!strongPassword(password))return Alert.alert('Password too weak','Use at least 8 characters with uppercase, lowercase and a number.');if(!acceptedLegal)return Alert.alert('Legal acceptance required','Accept the current Terms, Privacy Policy and Data Handling Policy to create the account.');setLoading(true);const{data,error}=await supabase.auth.updateUser({email:cleanEmail,password,data:{role:requestedRole,signup_complete:true}});if(error){setLoading(false);return Alert.alert('Could not finish account',error.message)}const userId=data.user?.id;if(!userId){setLoading(false);return Alert.alert('Account setup incomplete','Please try again.')}const profile=await supabase.from('profiles').update({email:cleanEmail,phone:normalizePhone(phone.trim())}).eq('id',userId);if(profile.error){setLoading(false);return Alert.alert('Could not save account details',profile.error.message)}const legal=await supabase.from('legal_acceptances').insert({profile_id:userId,terms_version:'2.0',privacy_version:'1.2',data_policy_version:'1.1'});if(legal.error){setLoading(false);return Alert.alert('Could not record legal acceptance',legal.error.message)}setLoading(false);router.replace({pathname:'/verify-email',params:{returnTo:requestedRole==='company'?'/onboarding?role=company':'/onboarding?role=professional'}})}
 async function signIn(){if(!supabase)return;const cleanEmail=email.trim().toLowerCase();if(!validEmail(cleanEmail))return Alert.alert('Email required','Enter your account email.');if(!password)return Alert.alert('Password required','Enter your password.');setLoading(true);const{data,error}=await supabase.auth.signInWithPassword({email:cleanEmail,password});setLoading(false);if(error)return Alert.alert('Sign in failed','Email or password is incorrect.');const role=(data.user.user_metadata?.role) as 'professional'|'company'|undefined;const{data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).maybeSingle();const finalRole=(profile?.role==='admin'?'admin':profile?.role||role);if(!data.user.email_confirmed_at)return router.replace({pathname:'/verify-email',params:{returnTo:finalRole==='admin'?'/admin':finalRole==='company'?'/company':'/home'}});const legal=await supabase.rpc('has_current_legal_acceptance',{p_profile_id:data.user.id});if(!legal.error&&!legal.data)return router.replace({pathname:'/legal-consent',params:{returnTo:finalRole==='admin'?'/admin':finalRole==='company'?'/company':'/home'}});router.replace(finalRole==='admin'?'/admin':finalRole==='company'?'/company':'/home')}
 async function googleSignup(){if(!supabase)return;if(!requestedRole)return Alert.alert('Choose an account type','Choose Professional or Company before starting Google signup.');if(step!==2)return Alert.alert('Phone verification required','Verify your phone first, then continue with Google.');if(!acceptedLegal)return Alert.alert('Legal acceptance required','Accept the current terms before continuing with Google.');const{data:current}=await supabase.auth.getUser();if(!current.user)return Alert.alert('Phone verification required','Verify your phone first.');setOauthBusy(true);const redirectTo=Linking.createURL('auth/callback');const{data,error}=await supabase.auth.linkIdentity({provider:'google',options:{redirectTo,queryParams:{access_type:'offline',prompt:'select_account'}}});if(error||!data?.url){setOauthBusy(false);return Alert.alert('Google sign-up unavailable',error?.message||'Could not start Google sign-up.')}const result=await WebBrowser.openAuthSessionAsync(data.url,redirectTo);if(result.type==='success'&&result.url){const parsed=Linking.parse(result.url);const codeValue=typeof parsed.queryParams?.code==='string'?parsed.queryParams.code:null;if(codeValue){const exchange=await supabase.auth.exchangeCodeForSession(codeValue);if(exchange.error){setOauthBusy(false);return Alert.alert('Google sign-up failed',exchange.error.message)}const user=exchange.data.user;await supabase.from('profiles').update({email:user.email||null,phone:normalizePhone(phone.trim())}).eq('id',user.id);await supabase.from('legal_acceptances').insert({profile_id:user.id,terms_version:'2.0',privacy_version:'1.2',data_policy_version:'1.1'});router.replace(requestedRole==='company'?'/onboarding?role=company':'/onboarding?role=professional')}}setOauthBusy(false)}
 return <SafeAreaView style={styles.safe}><KeyboardAvoidingView style={styles.flex} behavior={Platform.OS==='ios'?'padding':undefined}><ScrollView contentContainerStyle={styles.container} keyboardShouldPersistTaps="handled"><Pressable onPress={()=>step===2&&mode==='signup'?(setStep(1),setPassword('')):router.back()}><Text style={styles.back}>‹ Back</Text></Pressable><MediCrewLogo/><View style={styles.header}><Text style={styles.progress}>{mode==='signup'?`${step} / 2`:'SECURE SIGN IN'}</Text><Text style={styles.eyebrow}>{mode==='signup'?(step===1?'PHONE VERIFICATION':'ACCOUNT SECURITY'):'WELCOME BACK'}</Text><Text style={styles.title}>{mode==='signup'?(step===1?'Verify your phone.':'Finish your account.'):'Sign in to MediCrew.'}</Text><Text style={styles.subtitle}>{mode==='signup'?(step===1?'Your phone is verified first. Then choose email/password or Google.':'Your phone is verified. Finish with a verified email and password, or use Google.'):'Use your verified email address and password. Your phone number is not used to sign in.'}</Text></View><View style={styles.form}>{mode==='signin'?<><Text style={styles.label}>Email address</Text><TextInput autoCapitalize="none" keyboardType="email-address" autoCorrect={false} value={email} onChangeText={setEmail} placeholder="you@example.com" placeholderTextColor="#9AA19F" style={styles.input}/><Text style={styles.label}>Password</Text><View style={styles.passwordWrap}><TextInput autoCapitalize="none" autoCorrect={false} secureTextEntry={!showPassword} value={password} onChangeText={setPassword} placeholder="Your password" placeholderTextColor="#9AA19F" style={styles.passwordInput}/><Pressable onPress={()=>setShowPassword(x=>!x)}><Text style={styles.eye}>👁️</Text></Pressable></View><Pressable disabled={loading} onPress={signIn}><LinearGradient colors={[colors.greenStart,colors.green,colors.greenEnd]} style={[styles.button,loading&&styles.disabled]}><Text style={styles.buttonText}>{loading?'Signing in…':'Sign in'}</Text></LinearGradient></Pressable><Pressable style={styles.switch} onPress={()=>router.push('/forgot-password')}><Text style={styles.switchText}>Forgot your password?</Text></Pressable></>:step===1?<><Text style={styles.label}>Mobile number</Text><TextInput autoCapitalize="none" keyboardType="phone-pad" autoCorrect={false} value={phone} onChangeText={setPhone} placeholder="+33 6 12 34 56 78" placeholderTextColor="#9AA19F" style={styles.input} editable={!otpSent}/>{otpSent?<><Text style={styles.label}>Verification code</Text><TextInput keyboardType="number-pad" autoFocus value={code} onChangeText={setCode} placeholder="123456" placeholderTextColor="#9AA19F" style={styles.input} maxLength={8}/><Pressable disabled={loading} onPress={verifyPhone}><LinearGradient colors={[colors.greenStart,colors.green,colors.greenEnd]} style={[styles.button,loading&&styles.disabled]}><Text style={styles.buttonText}>{loading?'Verifying…':'Verify phone & continue'}</Text></LinearGradient></Pressable><Pressable disabled={loading} style={styles.switch} onPress={()=>{setOtpSent(false);setCode('')}}><Text style={styles.switchText}>Change phone number</Text></Pressable></>:<Pressable disabled={loading} onPress={sendCode}><LinearGradient colors={[colors.greenStart,colors.green,colors.greenEnd]} style={[styles.button,loading&&styles.disabled]}><Text style={styles.buttonText}>{loading?'Sending code…':'Send verification code'}</Text></LinearGradient></Pressable></>:<><View style={styles.stepBadge}><Text style={styles.stepBadgeText}>✓ PHONE VERIFIED</Text></View><Text style={styles.label}>Email address</Text><TextInput autoCapitalize="none" keyboardType="email-address" autoCorrect={false} value={email} onChangeText={setEmail} placeholder="you@example.com" placeholderTextColor="#9AA19F" style={styles.input}/><Text style={styles.label}>Password</Text><View style={styles.passwordWrap}><TextInput autoCapitalize="none" autoCorrect={false} secureTextEntry={!showPassword} value={password} onChangeText={setPassword} placeholder="At least 8 characters" placeholderTextColor="#9AA19F" style={styles.passwordInput}/><Pressable onPress={()=>setShowPassword(x=>!x)}><Text style={styles.eye}>👁️</Text></Pressable></View><View style={styles.rules}><Rule ok={password.length>=8} text="At least 8 characters"/><Rule ok={/[A-Z]/.test(password)} text="One uppercase letter"/><Rule ok={/[a-z]/.test(password)} text="One lowercase letter"/><Rule ok={/\d/.test(password)} text="One number"/></View><Pressable style={styles.legalRow} onPress={()=>setAcceptedLegal(x=>!x)}><View style={[styles.legalBox,acceptedLegal&&styles.legalBoxChecked]}><Text style={styles.legalTick}>{acceptedLegal?'✓':''}</Text></View><Text style={styles.legalText}>I agree to the current <Text style={styles.legalLink} onPress={()=>router.push('/terms')}>Terms</Text>, <Text style={styles.legalLink} onPress={()=>router.push('/privacy')}>Privacy</Text> and <Text style={styles.legalLink} onPress={()=>router.push('/data-policy')}>Data Policy</Text>.</Text></Pressable><Pressable disabled={loading} onPress={finishSignup}><LinearGradient colors={[colors.greenStart,colors.green,colors.greenEnd]} style={[styles.button,loading&&styles.disabled]}><Text style={styles.buttonText}>{loading?'Creating account…':'Create with email & password'}</Text></LinearGradient></Pressable><Pressable disabled={oauthBusy} onPress={googleSignup} style={[styles.googleButton,oauthBusy&&styles.disabled]}><Text style={styles.googleText}>{oauthBusy?'Opening Google…':'Continue with Google'}</Text></Pressable></>}{step===1&&mode==='signup'&&<Pressable style={styles.switch} onPress={()=>setMode('signin')}><Text style={styles.switchText}>Already have an account? Sign in with email</Text></Pressable>}{mode==='signin'&&<Pressable style={styles.switch} onPress={()=>{setMode('signup');setStep(1)}}><Text style={styles.switchText}>New to MediCrew? Create an account</Text></Pressable>}</View><Text style={styles.note}>Phone verification is required for new accounts. Sign-in uses email/password or Google; the phone number is not a sign-in credential.</Text></ScrollView></KeyboardAvoidingView></SafeAreaView>}
function Rule({ok,text}:{ok:boolean;text:string}){return <Text style={[styles.rule,ok&&styles.ruleOk]}>{ok?'✓':'○'} {text}</Text>}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:colors.paper},flex:{flex:1},container:{padding:24,paddingBottom:40,gap:18},back:{fontSize:16,fontWeight:'800',color:colors.ink},header:{marginTop:2},progress:{fontSize:10,fontWeight:'900',letterSpacing:1.3,color:colors.muted,marginBottom:7},eyebrow:{fontSize:11,fontWeight:'900',letterSpacing:1.5,color:colors.green},title:{fontSize:34,lineHeight:38,fontWeight:'900',color:colors.ink,letterSpacing:-1},subtitle:{fontSize:14,lineHeight:21,color:colors.muted,marginTop:10},form:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:radii.lg,padding:18},label:{fontSize:12,fontWeight:'800',color:colors.ink,marginTop:13,marginBottom:7},input:{height:52,borderWidth:1,borderColor:colors.line,borderRadius:radii.md,backgroundColor:colors.paper,paddingHorizontal:14,fontSize:16,color:colors.ink},passwordWrap:{height:52,borderWidth:1,borderColor:colors.line,borderRadius:radii.md,backgroundColor:colors.paper,flexDirection:'row',alignItems:'center',paddingRight:13},passwordInput:{flex:1,height:50,paddingHorizontal:14,fontSize:16,color:colors.ink},eye:{fontSize:20},button:{height:55,borderRadius:radii.md,alignItems:'center',justifyContent:'center',marginTop:19,overflow:'hidden'},disabled:{opacity:.55},buttonText:{color:colors.white,fontWeight:'900',fontSize:15},switch:{alignItems:'center',paddingVertical:12},switchText:{color:colors.greenDark,fontWeight:'800',fontSize:12},stepBadge:{alignSelf:'flex-start',backgroundColor:colors.greenSoft,borderRadius:radii.pill,paddingHorizontal:10,paddingVertical:7},stepBadgeText:{fontSize:10,fontWeight:'900',color:colors.greenDark,letterSpacing:.7},rules:{marginTop:10,gap:4},rule:{fontSize:11,color:colors.muted},ruleOk:{color:colors.greenDark,fontWeight:'700'},legalRow:{flexDirection:'row',alignItems:'flex-start',marginTop:16},legalBox:{width:22,height:22,borderRadius:6,borderWidth:1.5,borderColor:'#C8CFCA',alignItems:'center',justifyContent:'center',marginRight:10},legalBoxChecked:{backgroundColor:colors.green,borderColor:colors.green},legalTick:{color:colors.white,fontWeight:'900'},legalText:{flex:1,fontSize:11,lineHeight:17,color:colors.muted},legalLink:{color:colors.greenDark,fontWeight:'800'},googleButton:{height:55,borderRadius:radii.md,alignItems:'center',justifyContent:'center',marginTop:10,borderWidth:1,borderColor:colors.line,backgroundColor:colors.white},googleText:{color:colors.ink,fontWeight:'900',fontSize:14},note:{fontSize:11,lineHeight:17,color:colors.muted,textAlign:'center'}});