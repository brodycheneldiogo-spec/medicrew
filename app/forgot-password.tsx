import { useState } from 'react';
import { Alert, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { router } from 'expo-router';
import { colors, radii } from '../lib/theme';
import { supabase } from '../lib/supabase';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[loading,setLoading]=useState(false);const[sent,setSent]=useState(false);async function send(){if(!supabase)return;const value=email.trim().toLowerCase();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value))return Alert.alert('Email required','Enter a valid email address.');setLoading(true);const{error}=await supabase.auth.resetPasswordForEmail(value,{redirectTo:'medicrew://reset-password'});setLoading(false);if(error)return Alert.alert('Could not send reset email',error.message);setSent(true)}return <SafeAreaView style={s.safe}><View style={s.container}><Pressable onPress={()=>router.back()}><Text style={s.back}>‹ Back</Text></Pressable><Text style={s.eyebrow}>ACCOUNT RECOVERY</Text><Text style={s.title}>Reset your password.</Text><Text style={s.sub}>{sent?'Check your inbox for a secure password reset link.':'Enter the email attached to your MediCrew account.'}</Text>{!sent&&<><Text style={s.label}>Email address</Text><TextInput autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="you@example.com" style={s.input}/><Pressable disabled={loading} onPress={send} style={s.primary}><Text style={s.primaryText}>{loading?'Sending…':'Send reset link'}</Text></Pressable></>}{sent&&<Pressable onPress={()=>router.replace('/auth')} style={s.primary}><Text style={s.primaryText}>Return to sign in</Text></Pressable>}</View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.paper},container:{flex:1,padding:24,justifyContent:'center'},back:{fontSize:15,fontWeight:'800',color:colors.ink,marginBottom:35},eyebrow:{fontSize:11,fontWeight:'900',letterSpacing:1.5,color:colors.green},title:{fontSize:35,fontWeight:'900',color:colors.ink,marginTop:8},sub:{fontSize:14,lineHeight:21,color:colors.muted,marginTop:9,marginBottom:24},label:{fontSize:12,fontWeight:'800',color:colors.ink,marginBottom:7},input:{height:52,borderWidth:1,borderColor:colors.line,borderRadius:radii.md,backgroundColor:colors.white,paddingHorizontal:14,fontSize:16,color:colors.ink},primary:{height:55,borderRadius:radii.md,backgroundColor:colors.ink,alignItems:'center',justifyContent:'center',marginTop:16},primaryText:{color:colors.white,fontSize:15,fontWeight:'900'}});
