import { useEffect, useMemo, useState } from 'react';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { router } from 'expo-router';
import { colors, radii } from '../../lib/theme';
import { skillCatalog, skillLevels, SkillLevel } from '../../lib/professional';
import { supabase } from '../../lib/supabase';
import { usePreferences } from '../../lib/preferences-context';
import { localize } from '../../lib/i18n';
type StoredSkill={slug:string;level:SkillLevel;years?:number};type SaveResult={saved_count?:number;slugs?:string[]};
export default function Skills(){const prefs=usePreferences();const L=(en:string,fr:string,es:string)=>localize(prefs.language,en,fr,es);const[selected,setSelected]=useState<Record<string,SkillLevel>>({});const[years,setYears]=useState<Record<string,number>>({});const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[savedCount,setSavedCount]=useState<number|null>(null);const[error,setError]=useState('');const groups=useMemo(()=>Array.from(new Set(skillCatalog.map(s=>s.category))),[]);const load=async()=>{if(!supabase){setError('Supabase');setLoading(false);return}const{data,error:rpcError}=await supabase.rpc('get_my_professional_details');if(rpcError){setError(rpcError.message);setLoading(false);return}const next:Record<string,SkillLevel>={};const nextYears:Record<string,number>={};for(const item of ((data?.skills||[])as StoredSkill[])){if(skillCatalog.some(skill=>skill.id===item.slug)){next[item.slug]=item.level;nextYears[item.slug]=Number(item.years||0)}}setSelected(next);setYears(nextYears);setSavedCount(null);setLoading(false)};useEffect(()=>{void load()},[]);const save=async()=>{if(!supabase)return;setSaving(true);setSavedCount(null);setError('');const skills=Object.entries(selected).map(([slug,level])=>({slug,level,years:Math.max(0,Number(years[slug]||0))}));const{data,error:saveError}=await supabase.rpc('replace_my_skills_v2',{p_skills:skills});if(saveError){setError(saveError.message);setSaving(false);return}const result=(data||{})as SaveResult;const expected=skills.length;const actual=Number(result.saved_count??-1);if(actual!==expected){setError(L('Save verification failed.','Échec de la vérification de l’enregistrement.','Falló la verificación del guardado.'));setSaving(false);return}setSavedCount(actual);setSaving(false)};const setSkillYear=(slug:string,value:string)=>{const normalized=value.replace(',','.').replace(/[^0-9.]/g,'');const numeric=normalized===''?0:Math.min(60,Math.max(0,Number(normalized)));setYears({...years,[slug]:Number.isFinite(numeric)?numeric:0});setSavedCount(null);setError('')};return <SafeAreaView style={styles.safe}><View style={styles.screen}><ScrollView contentContainerStyle={styles.container}><Pressable onPress={()=>router.back()}><Text style={styles.back}>‹ {prefs.tr('profile')}</Text></Pressable><Text style={styles.eyebrow}>{L('CLINICAL PROFILE','PROFIL CLINIQUE','PERFIL CLÍNICO')}</Text><Text style={styles.title}>{L('Skills & experience','Compétences & expérience','Habilidades y experiencia')}</Text><Text style={styles.sub}>{L('Add only skills you can accurately demonstrate. Verified skills may be used for mission eligibility.','Ajoutez uniquement les compétences que vous pouvez réellement démontrer. Les compétences vérifiées peuvent servir à l’éligibilité des missions.','Añade solo habilidades que puedas demostrar. Las habilidades verificadas pueden usarse para la elegibilidad de misiones.')}</Text>{error?<View style={styles.error}><Text style={styles.errorText}>{error}</Text></View>:null}{loading?<View style={styles.loading}><ActivityIndicator size="large" color={colors.green}/><Text style={styles.muted}>{L('Loading your skills…','Chargement des compétences…','Cargando habilidades…')}</Text></View>:groups.map(category=><View key={category} style={styles.group}><Text style={styles.groupTitle}>{category}</Text>{skillCatalog.filter(s=>s.category===category).map(skill=><View key={skill.id} style={styles.skill}><View style={styles.skillHead}><Text style={styles.skillName}>{skill.name}</Text>{selected[skill.id]?<Text style={styles.selected}>{L('ADDED','AJOUTÉ','AÑADIDO')}</Text>:null}</View><View style={styles.levels}>{skillLevels.map(level=><Pressable key={level.id} onPress={()=>{setSelected({...selected,[skill.id]:level.id as SkillLevel});setSavedCount(null);setError('')}} style={[styles.level,selected[skill.id]===level.id&&styles.levelActive]}><Text style={[styles.levelText,selected[skill.id]===level.id&&styles.levelTextActive]}>{level.label}</Text></Pressable>)}</View>{selected[skill.id]?<View style={styles.yearsRow}><Text style={styles.yearsLabel}>{L('Years of experience','Années d’expérience','Años de experiencia')}</Text><View style={styles.yearsInputWrap}><TextInput value={years[skill.id]===undefined?'':String(years[skill.id])} onChangeText={value=>setSkillYear(skill.id,value)} keyboardType="decimal-pad" inputMode="decimal" placeholder="0" style={styles.yearsInput}/><Text style={styles.yearsSuffix}>{L('years','ans','años')}</Text></View></View>:null}</View>)}</View>)}</ScrollView><View style={styles.footer}>{savedCount!==null?<Text style={styles.saved}>✓ {savedCount} {L('skills saved','compétences enregistrées','habilidades guardadas')}</Text>:null}<Pressable disabled={loading||saving} style={[styles.button,(loading||saving)&&styles.disabled]} onPress={save}>{saving?<ActivityIndicator color={colors.white}/>:<Text style={styles.buttonText}>{L('Save skills','Enregistrer les compétences','Guardar habilidades')}</Text>}</Pressable></View></View></SafeAreaView>}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:colors.paper},screen:{flex:1},container:{padding:24,paddingBottom:30},back:{fontSize:16,fontWeight:'800',color:colors.ink,marginBottom:25},eyebrow:{fontSize:11,fontWeight:'900',letterSpacing:1.4,color:colors.green},title:{fontSize:35,fontWeight:'800',color:colors.ink,letterSpacing:-1.1,marginTop:7},sub:{fontSize:14,lineHeight:21,color:colors.muted,marginTop:9},group:{marginTop:26},groupTitle:{fontSize:16,fontWeight:'800',color:colors.ink,marginBottom:9},skill:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:radii.md,padding:14,marginBottom:9},skillHead:{flexDirection:'row',justifyContent:'space-between',alignItems:'center'},skillName:{fontSize:13,fontWeight:'800',color:colors.ink,flex:1},selected:{fontSize:8,fontWeight:'900',letterSpacing:.8,color:colors.green},levels:{flexDirection:'row',gap:6,marginTop:11},level:{paddingHorizontal:9,paddingVertical:7,borderRadius:radii.pill,borderWidth:1,borderColor:colors.line},levelActive:{backgroundColor:colors.ink,borderColor:colors.ink},levelText:{fontSize:9,fontWeight:'700',color:colors.muted},levelTextActive:{color:colors.white},yearsRow:{marginTop:12,flexDirection:'row',alignItems:'center',justifyContent:'space-between',gap:12},yearsLabel:{fontSize:11,fontWeight:'700',color:colors.muted,flex:1},yearsInputWrap:{height:38,minWidth:112,borderWidth:1,borderColor:colors.line,borderRadius:10,backgroundColor:colors.paper,flexDirection:'row',alignItems:'center',paddingHorizontal:10},yearsInput:{flex:1,fontSize:13,fontWeight:'800',color:colors.ink,paddingVertical:0},yearsSuffix:{fontSize:10,color:colors.muted,fontWeight:'700'},button:{height:54,borderRadius:radii.md,backgroundColor:colors.ink,alignItems:'center',justifyContent:'center'},disabled:{opacity:.55},buttonText:{fontSize:16,fontWeight:'800',color:colors.white},footer:{padding:16,paddingBottom:20,borderTopWidth:1,borderTopColor:colors.line,backgroundColor:colors.paper},saved:{textAlign:'center',fontSize:12,fontWeight:'800',color:colors.green,marginBottom:8},error:{marginTop:16,padding:12,borderRadius:radii.md,backgroundColor:'#FDECEC'},errorText:{fontSize:12,color:'#A33A3A'},loading:{paddingVertical:40,alignItems:'center',gap:10},muted:{fontSize:12,color:colors.muted}});