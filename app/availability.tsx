import { useEffect, useMemo, useState } from 'react';
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { router } from 'expo-router';
import { colors, radii } from '../lib/theme';
import { supabase } from '../lib/supabase';
const transportOptions=[['commercial-flight','Flight'],['ground-transport','Ground'],['air-ambulance','Air ambulance'],['train','Train']] as const;
const key=(d:Date)=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`};const parseLocal=(date:string,time:string)=>new Date(`${date}T${time}:00`);
export default function Availability(){const[slots,setSlots]=useState<any[]>([]),[selected,setSelected]=useState(key(new Date())),[start,setStart]=useState('10:00'),[end,setEnd]=useState('18:00'),[location,setLocation]=useState(''),[international,setInternational]=useState(false),[transport,setTransport]=useState<string[]>(['commercial-flight','ground-transport','air-ambulance']),[saving,setSaving]=useState(false),[loading,setLoading]=useState(true);const days=useMemo(()=>Array.from({length:42},(_,i)=>{const d=new Date();d.setHours(0,0,0,0);d.setDate(d.getDate()+i);return d}),[]);const selectedSlot=slots.find(x=>key(new Date(x.starts_at))===selected);useEffect(()=>{load()},[]);useEffect(()=>{if(selectedSlot){const a=new Date(selectedSlot.starts_at),b=new Date(selectedSlot.ends_at);setStart(a.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',hour12:false}));setEnd(b.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',hour12:false}));setLocation(selectedSlot.start_location||'');setInternational(!!selectedSlot.international);setTransport(Array.isArray(selectedSlot.transport_types)&&selectedSlot.transport_types.length?selectedSlot.transport_types:transport)}},[selectedSlot]);async function load(){if(!supabase){setLoading(false);return}try{const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Session expired');const{data,error}=await supabase.from('professional_availability').select('id,starts_at,ends_at,start_location,max_notice_hours,international,transport_types').eq('professional_id',user.id).gte('ends_at',new Date().toISOString()).order('starts_at');if(error)throw error;setSlots(data||[]);if(data?.[0])setSelected(key(new Date(data[0].starts_at)))}catch(e:any){Alert.alert('Calendar unavailable',e?.message||'Unable to load availability')}finally{setLoading(false)}}function toggleTransport(v:string){setTransport(a=>a.includes(v)?a.filter(x=>x!==v):[...a,v])}async function save(){if(!supabase)return;if(!location.trim())return Alert.alert('Starting location required','Add the city or area where you can start.');if(!transport.length)return Alert.alert('Transport type required','Select at least one transport type.');const a=parseLocal(selected,start),b=parseLocal(selected,end);if(Number.isNaN(a.getTime())||Number.isNaN(b.getTime())||b<=a)return Alert.alert('Invalid hours','End time must be after start time.');if(a<=new Date())return Alert.alert('Date unavailable','Choose a future day and time.');setSaving(true);const{error}=await supabase.rpc('upsert_my_availability_slot',{p_slot_id:selectedSlot?.id||null,p_starts_at:a.toISOString(),p_ends_at:b.toISOString(),p_start_location:location.trim(),p_max_notice_hours:3,p_international:international,p_transport_types:transport});setSaving(false);if(error)return Alert.alert('Could not save',error.message);await load();Alert.alert('Calendar saved','This availability is now part of your matching calendar.')}async function remove(){if(!supabase||!selectedSlot)return;setSaving(true);const{error}=await supabase.rpc('delete_my_availability_slot',{p_slot_id:selectedSlot.id});setSaving(false);if(error)return Alert.alert('Could not remove',error.message);await load()}if(loading)return <SafeAreaView style={s.safe}><ActivityIndicator style={{marginTop:80}} color={colors.green}/></SafeAreaView>;return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.container}><Pressable onPress={()=>router.back()}><Text style={s.back}>‹ Back</Text></Pressable><Text style={s.eyebrow}>REAL AVAILABILITY CALENDAR</Text><Text style={s.title}>Your calendar.</Text><Text style={s.sub}>Add multiple future availability slots. Matching uses these exact time windows, not a simple online toggle.</Text><View style={s.month}><Text style={s.monthTitle}>{days[0].toLocaleDateString([], {month:'long',year:'numeric'})}</Text><View style={s.weekdays}>{['M','T','W','T','F','S','S'].map((x,i)=><Text key={i} style={s.weekday}>{x}</Text>)}</View><View style={s.grid}>{days.map(d=>{const k=key(d),has=slots.some(x=>key(new Date(x.starts_at))===k);return <Pressable key={k} onPress={()=>setSelected(k)} style={[s.day,k===selected&&s.dayActive,has&&s.dayHas]}><Text style={[s.dayText,k===selected&&s.dayTextActive]}>{d.getDate()}</Text>{has?<View style={[s.dot,k===selected&&{backgroundColor:colors.white}]}/>:null}</Pressable>})}</View></View><View style={s.card}><Text style={s.cardTitle}>{new Date(`${selected}T12:00:00`).toLocaleDateString([], {weekday:'long',day:'numeric',month:'long'})}</Text><Text style={s.label}>Start time</Text><TextInput value={start} onChangeText={setStart} keyboardType="numbers-and-punctuation" placeholder="10:00" style={s.input}/><Text style={s.label}>End time</Text><TextInput value={end} onChangeText={setEnd} keyboardType="numbers-and-punctuation" placeholder="18:00" style={s.input}/><Text style={s.label}>Starting location</Text><TextInput value={location} onChangeText={setLocation} placeholder="City or area" style={s.input}/><Text style={s.label}>Transport</Text><View style={s.wrap}>{transportOptions.map(([id,label])=><Pressable key={id} onPress={()=>toggleTransport(id)} style={[s.choice,transport.includes(id)&&s.choiceActive]}><Text style={[s.choiceText,transport.includes(id)&&s.choiceTextActive]}>{label}</Text></Pressable>)}</View><Pressable onPress={()=>setInternational(v=>!v)} style={s.switch}><View style={{flex:1}}><Text style={s.switchTitle}>International missions</Text><Text style={s.switchSub}>{international?'Enabled':'Disabled'}</Text></View><View style={[s.toggle,international&&s.toggleOn]}><View style={[s.knob,!international&&s.knobOff]}/></View></Pressable><Pressable disabled={saving} onPress={save} style={s.primary}><Text style={s.primaryText}>{saving?'Saving…':selectedSlot?'Update availability':'Add availability'}</Text></Pressable>{selectedSlot&&<Pressable disabled={saving} onPress={remove} style={s.delete}><Text style={s.deleteText}>Remove this slot</Text></Pressable>}</View></ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.paper},container:{padding:22,paddingBottom:50},back:{fontSize:15,fontWeight:'800',color:colors.ink,marginBottom:24},eyebrow:{fontSize:10,fontWeight:'900',letterSpacing:1.5,color:colors.green},title:{fontSize:34,fontWeight:'900',color:colors.ink,marginTop:7},sub:{fontSize:13,lineHeight:20,color:colors.muted,marginTop:7},month:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:radii.lg,padding:15,marginTop:20},monthTitle:{fontSize:16,fontWeight:'900',color:colors.ink,marginBottom:12,textTransform:'capitalize'},weekdays:{flexDirection:'row',marginBottom:6},weekday:{width:'14.285%',textAlign:'center',fontSize:10,fontWeight:'900',color:colors.muted},grid:{flexDirection:'row',flexWrap:'wrap'},day:{width:'14.285%',height:43,alignItems:'center',justifyContent:'center',borderRadius:11,marginBottom:4},dayActive:{backgroundColor:colors.ink},dayHas:{backgroundColor:colors.greenSoft},dayText:{fontSize:12,fontWeight:'800',color:colors.ink},dayTextActive:{color:colors.white},dot:{width:5,height:5,borderRadius:3,backgroundColor:colors.green,marginTop:2},card:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:radii.lg,padding:17,marginTop:14},cardTitle:{fontSize:17,fontWeight:'900',color:colors.ink,textTransform:'capitalize',marginBottom:8},label:{fontSize:11,fontWeight:'800',color:colors.muted,marginTop:10,marginBottom:5},input:{height:46,borderWidth:1,borderColor:colors.line,borderRadius:12,paddingHorizontal:12,fontSize:14,color:colors.ink},wrap:{flexDirection:'row',flexWrap:'wrap',gap:7},choice:{borderWidth:1,borderColor:colors.line,borderRadius:10,paddingHorizontal:11,paddingVertical:9},choiceActive:{backgroundColor:colors.ink,borderColor:colors.ink},choiceText:{fontSize:11,fontWeight:'800',color:colors.ink},choiceTextActive:{color:colors.white},switch:{marginTop:15,padding:13,borderWidth:1,borderColor:colors.line,borderRadius:12,flexDirection:'row',alignItems:'center'},switchTitle:{fontSize:13,fontWeight:'800',color:colors.ink},switchSub:{fontSize:10,color:colors.muted,marginTop:2},toggle:{width:44,height:25,borderRadius:14,backgroundColor:'#B9C0BD',padding:3},toggleOn:{backgroundColor:colors.green},knob:{width:19,height:19,borderRadius:10,backgroundColor:colors.white,alignSelf:'flex-end'},knobOff:{alignSelf:'flex-start'},primary:{height:53,borderRadius:14,backgroundColor:colors.ink,alignItems:'center',justifyContent:'center',marginTop:15},primaryText:{color:colors.white,fontSize:14,fontWeight:'900'},delete:{height:44,alignItems:'center',justifyContent:'center'},deleteText:{color:'#A83E3E',fontSize:12,fontWeight:'800'}});
